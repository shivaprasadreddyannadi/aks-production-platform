apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: platform-app

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: platform-app

  endpoints:
    - port: http
      interval: 30s