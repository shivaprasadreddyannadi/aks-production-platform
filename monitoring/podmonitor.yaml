apiVersion: monitoring.coreos.com/v1
kind: PodMonitor

metadata:
  name: platform-app

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: platform-app

  podMetricsEndpoints:
    - port: http
      interval: 30s